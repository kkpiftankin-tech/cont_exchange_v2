name: cpp-build

on:
  pull_request:
    paths:
      - "cpp/**"
      - "contracts/**"
      - "CMakeLists.txt"
      - "docker/Dockerfile.service"
      - ".github/workflows/cpp-build.yml"
  push:
    branches: [main]
    paths:
      - "cpp/**"
      - "contracts/**"
      - "CMakeLists.txt"
      - "docker/Dockerfile.service"
      - ".github/workflows/cpp-build.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          # F-05A / ADR-048 §7: OSQP (QP solver для matching) подтягивается через
          # CMake FetchContent (pinned v0.6.3) — apt-пакет не нужен; требуются git +
          # сеть на configure (уже используются clickhouse-cpp FetchContent). Eigen —
          # ниже (libeigen3-dev) — собирает W/D. Держать синхронно с matching CMake и
          # docker/Dockerfile.service.
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            pkg-config \
            git \
            ca-certificates \
            libgrpc++-dev \
            protobuf-compiler-grpc \
            libprotobuf-dev \
            libabsl-dev \
            librdkafka-dev \
            libboost-all-dev \
            libeigen3-dev \
            libpqxx-dev \
            libpq-dev \
            libcurl4-openssl-dev \
            libssl-dev \
            libasio-dev \
            nlohmann-json3-dev

      - name: Show toolchain versions
        run: |
          cmake --version
          g++ --version
          protoc --version
          pkg-config --modversion grpc++
          pkg-config --modversion rdkafka

      - name: Configure (Release)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build all targets
        run: cmake --build build -j "$(nproc)"

      - name: Verify binaries produced
        run: |
          set -e
          for svc in gateway order_flow matching risk ledger market_data venues observability; do
            test -x "build/bin/${svc}" || { echo "MISSING: build/bin/${svc}"; exit 1; }
            echo "OK build/bin/${svc}"
          done

      # Тестов в проекте пока нет (см. T-F04-170 в docs/implementation-plan/F-04-batch-clearing.tasks.md).
      # ctest на пустом наборе тестов завершается успешно; шаг оставлен заготовкой.
      # Снять continue-on-error и поднять --output-on-failure до hard-fail, когда появятся unit-тесты.
      - name: Run tests (placeholder until T-F04-170)
        run: ctest --test-dir build --output-on-failure
        continue-on-error: true
