# IN-004 — F-02. Создание одноактивной потоковой заявки (FlowOrder) — v2

## Source File

[F-02 Create FlowOrder v2.md](F-02 Create FlowOrder v2.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (обновление существующей фичи) — **F-02**. Существующие docs: `docs/02-system/features/F-02-create-floworder/`.

## Processing Status

`segmenting` (зарегистрирован; fragment-map создан по заголовкам; нормализация в целевые docs — на этапе полного внедрения по этой фиче).

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-02-create-floworder/` (feature.yaml + README, merge),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-02-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-02-test-plan.md`, `docs/implementation-plan/F-02-*.tasks.md`.

## Conflict Notes

⚠️ Фича F-02 уже частично документирована. Нормализация — **merge** (read → preserve → merge → append source refs), НЕ перезапись. Расхождения новой версии с текущими docs фиксировать как `Conflict Notes` в затронутых артефактах; архитектурные — ADR.

## Notes

Обновлённая v2 уже документированной фичи F-02. Имеет '## Приложение: реестр всех исправлений' — список правок относительно v1. Нормализация = merge в существующие docs F-02 + Conflict Notes на расхождения.

