# IN-005 — F-04. Batch Clearing Cycle — v1 (чистый исходник)

## Source File

[F‑04. Batch Clearing v1.md](F‑04. Batch Clearing v1.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (исходник; дубль к уже ингестированному) — **F-04**. Существующие docs: `docs/02-system/features/F-04-batch-clearing/`.

## Processing Status

`segmenting` (зарегистрирован; fragment-map создан по заголовкам; нормализация в целевые docs — на этапе полного внедрения по этой фиче).

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-04-batch-clearing/` (feature.yaml + README, merge),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-04-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-04-test-plan.md`, `docs/implementation-plan/F-04-*.tasks.md`.

## Conflict Notes

⚠️ Фича F-04 уже частично документирована. Нормализация — **merge** (read → preserve → merge → append source refs), НЕ перезапись. Расхождения новой версии с текущими docs фиксировать как `Conflict Notes` в затронутых артефактах; архитектурные — ADR.

## Notes

Чистый (не-mojibake) исходник F-04. Пересекается с IN-003 (тот же feature, декодированный вручную из mojibake). Служит byte-faithful первоисточником; расхождения с IN-003 → Conflict Notes. Полная версия с диаграммами уже собрана в 2026-08-17-F-04-batch-clearing-v1-full.md.

