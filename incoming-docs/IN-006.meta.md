# IN-006 — F-06. Просмотр позиций, PnL и маржи

## Source File

[F‑06. Просмотр позиций, PnL и маржи.md](F‑06. Просмотр позиций, PnL и маржи.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (исходник существующей фичи) — **F-06**. Существующие docs: `docs/02-system/features/F-06-positions-pnl-margin/`.

## Processing Status

`segmenting` (зарегистрирован; fragment-map создан по заголовкам; нормализация в целевые docs — на этапе полного внедрения по этой фиче).

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-06-positions-pnl-margin/` (feature.yaml + README, merge),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-06-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-06-test-plan.md`, `docs/implementation-plan/F-06-*.tasks.md`.

## Conflict Notes

⚠️ Фича F-06 уже частично документирована. Нормализация — **merge** (read → preserve → merge → append source refs), НЕ перезапись. Расхождения новой версии с текущими docs фиксировать как `Conflict Notes` в затронутых артефактах; архитектурные — ADR.

## Notes

Исходник уже документированной F-06. Нормализация = merge/дополнение существующих docs F-06.

