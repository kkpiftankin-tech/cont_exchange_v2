# IN-007 — F-11. Подключение внешних площадок (CEX/DEX) — corrected v1

## Source File

[F-11_corrected v1.md](F-11_corrected v1.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (обновление существующей фичи) — **F-11**. Существующие docs: `docs/02-system/features/F-11-external-venues-lob-to-fob/`.

## Processing Status

`segmenting` (зарегистрирован; fragment-map создан по заголовкам; нормализация в целевые docs — на этапе полного внедрения по этой фиче).

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-11-external-venues-lob-to-fob/` (feature.yaml + README, merge),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-11-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-11-test-plan.md`, `docs/implementation-plan/F-11-*.tasks.md`.

## Conflict Notes

⚠️ Фича F-11 уже частично документирована. Нормализация — **merge** (read → preserve → merge → append source refs), НЕ перезапись. Расхождения новой версии с текущими docs фиксировать как `Conflict Notes` в затронутых артефактах; архитектурные — ADR.

## Notes

Corrected v1 фичи F-11 (194KB, самый крупный). Содержит circuit-breaker, DEX/AMM без стакана, JSON-модель, REST API, venue.health. Merge в docs F-11 + возможные ADR по circuit-breaker/health.

