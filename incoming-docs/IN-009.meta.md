# IN-009 — F-15. Backtest / Replay — v1

## Source File

[F-15. Backtest   Replay v1.md](F-15. Backtest   Replay v1.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (исходник существующей фичи) — **F-15**. Существующие docs: `docs/02-system/features/F-15-backtest-replay/`.

## Processing Status

`segmenting` (зарегистрирован; fragment-map создан по заголовкам; нормализация в целевые docs — на этапе полного внедрения по этой фиче).

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-15-backtest-replay/` (feature.yaml + README, merge),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-15-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-15-test-plan.md`, `docs/implementation-plan/F-15-*.tasks.md`.

## Conflict Notes

⚠️ Фича F-15 уже частично документирована. Нормализация — **merge** (read → preserve → merge → append source refs), НЕ перезапись. Расхождения новой версии с текущими docs фиксировать как `Conflict Notes` в затронутых артефактах; архитектурные — ADR.

## Notes

Исходник F-15 (198KB). Определяет replay-движок, детерминизм, archive/replay контракты. Совпадает с текущей in-progress реализацией cpp/backtest; merge + сверка.

