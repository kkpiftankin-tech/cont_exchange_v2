# IN-010 — F-20. Live Venue Simulator — симулятор исполнения на внешних площадках

## Source File

[F-20 -Live-Venue-Simulator.md](F-20 -Live-Venue-Simulator.md) — immutable архив, зарегистрирован 2026-08-18. Кодировка: читаемая UTF-8 (не mojibake).

## Document Type

Feature spec (НОВАЯ фича) — **F-20**. В `docs/` фичи ещё нет (greenfield).

## Processing Status

`ingested (docs-only)` — полный docs-as-code chain создан 2026-08-18: feature.yaml+README, UC-F20-01/02 + L0/L1 sequences, FR-F20/NFR-F20, entities+business-rules §F-20, contracts (sim-topics + admin REST), data-доки (sim-sessions/sim-execution-reports), test-plan, implementation-plan, coverage-matrix + feature-index. Код/infra/proto НЕ трогались (по запросу — только документация). ВАЖНО: базовый симулятор подтверждения хеджа уже реализован в cpp/venues/venues_loop.cpp (intent→filled report) — F-20 его расширяет; см. CN-F20-04. Остаток вне docs: ADR (топики/proto/sim-book) + код.

## Target Areas

Итоговое размещение — по classification во фрагмент-карте. Основные зоны:

- `docs/02-system/features/F-20-live-venue-simulator/` (create feature.yaml + README),
- `docs/02-system/{functional,non-functional}-requirements.md`,
- `docs/02-system/use-cases/UC-F-20-*/` + sequences (L0/L1),
- `docs/06-api/{grpc,messaging,rest}/` (contracts), `docs/07-data/` (schemas),
- `docs/10-testing/features/F-20-test-plan.md`, `docs/implementation-plan/F-20-*.tasks.md`.

## Conflict Notes

Новая фича — конфликтов с существующими docs нет. Проверить границы с F-11 (venue-модель) и F-12 (execution.intents/reports), чтобы не дублировать ответственность.

## Notes

НОВАЯ фича F-20 (в docs нет). Симулятор внешнего venue: accept/reject/partial/timeout child-order'ов, интеграция с F-12 execution.intents/reports и F-11 venue-моделью. ПОЛНОЕ внедрение — первым (greenfield).

