# Kubernetes manifests (starter)

These manifests are **minimal placeholders** to show how each microservice can be deployed.

For production you will likely:
- use Helm/Kustomize
- add Secrets, ConfigMaps, HPA, PDB
- add Ingress for `gateway`
- use a managed Kafka (or Redpanda operator)
