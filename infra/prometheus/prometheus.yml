global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: matching
    static_configs:
      - targets:
          - matching:8081

  - job_name: backtest
    static_configs:
      - targets:
          - backtest:8087
